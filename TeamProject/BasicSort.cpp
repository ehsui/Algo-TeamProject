/**
 * @file BasicSort.cpp
 * @brief 템플릿 정렬 알고리즘 (빈 파일)
 * 
 * 모든 구현은 BasicSort.hpp 헤더 파일에 있습니다.
 * 템플릿 함수는 헤더에 구현이 있어야 하기 때문입니다.
 * 
 * 이 파일은 프로젝트 빌드 설정 호환성을 위해 유지됩니다.
 */

// 헤더만 포함 (명시적 인스턴스화가 필요하면 여기에 추가)
#include "BasicSort.hpp"
#include "Video.h"

// Score와 key 타입에 대한 명시적 인스턴스화 (선택적)
// 이렇게 하면 컴파일 시간이 줄어들 수 있음

// template void selectsort<Score>(vector<Score>&);
// template void selectsort<key>(vector<key>&);
// ... 기타 함수들
